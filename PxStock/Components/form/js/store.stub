$(document).ready(function(){
    if ($('#frm{{model}}{{lastNameSpace}}Store').length > 0) {
        let rules = {
            name: {
                required: true,
                maxlength: 253
            }
        };
        PX.ajaxRequest({
            element: 'frm{{model}}{{lastNameSpace}}Store',
            validation: true,
            script: '{{nameDown}}/{{uri}}/store',
            rules,
            afterSuccess: {
                type: 'inflate_reset_response_data',
            }
        });
    }
});
