$(document).ready(function(){

    if ($('#frmUpdate{{model}}Permission').length > 0) {
        let rules = {
        };
        PX?.ajaxRequest({
            element: 'frmUpdate{{model}}Permission',
            validation: true,
            script: '{{nameDown}}/hrm/user/user-policy',
            rules,
            afterSuccess: {
                type: 'inflate_redirect_response_data',
            }
        });
    }
});
