<?php

use App\Http\Controllers\{{importNameSpace}}\Form\{{controller}};
use Illuminate\Support\Facades\Route;
//vpx_imports
Route::prefix('{{prefix}}')->group(function(){
    Route::get('{{nameSpaceDown}}',[{{controller}}::class,'index']);
    Route::post('{{nameSpaceDown}}',[{{controller}}::class,'store']);
    //vpx_attach
});
